<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Modifier l'agenda</title>
    <link rel="icon" href="/img/favicon.ico" type="image/x-icon">
</head>

<body>
    <h1>Modification des agendas</h1>
    <% if(locals.errMsg) { %><% "<p>" %><%= errMsg %><% "</p>" %><% }%>
    <div>
        <form method="post">
            <% for (agenda of agendas) {%>
            <label for="<%= agenda.id %>"><%= agenda.nom %> : </label>
                <input id="<%= agenda.id %>" type="text" name="<%= agenda.id %>" value="<%= agenda.nom %>" required><br>
            <% } %>
            <button type="submit">Envoyer</button>
            <button type="button" onclick="window.location.href = '/'">Annuler</button>
        </form>

    </div>
</body>

</html>