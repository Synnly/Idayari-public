<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('partials/head.ejs') %>
    <link href="/css/index.css" rel="stylesheet">
</head>

<body class="container flex-column d-flex min-vh-100 min-vw-100 m-0 p-0" data-bs-theme="light">
    <%- include('partials/header.ejs') %>
    <% if(locals.errMsg) { %><% "<p>" %><%= errMsg %><% "</p>" %><% }%>
    <div class="container p-3 d-flex flex-column align-items-center flex-grow-1">
        <form class="row needs-validation w-100" method="post" novalidate>
            <% for (agenda_id of Object.keys(locals.agendas)) {%>
                <% const agenda = locals.agendas[agenda_id]; %>
                <% // si ce n'est pas son agenda, on ne l'affiche pas %>
                <% if (!agenda.isOwner) { %>
                    <% continue; %>
                <% } %>
                <div class="mb-3 w-100" id="div_<%= agenda_id %>">
                    <div class="d-flex">
                        <button class="btn btn-danger me-2 h-fit-content" onclick="confirmerSuppression('<%= agenda_id %>')">
                                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-trash" viewBox="0 0 16 16">
                                    <path d="M5.5 5.5A.5.5 0 0 1 6 6v6a.5.5 0 0 1-1 0V6a.5.5 0 0 1 .5-.5m2.5 0a.5.5 0 0 1 .5.5v6a.5.5 0 0 1-1 0V6a.5.5 0 0 1 .5-.5m3 .5a.5.5 0 0 0-1 0v6a.5.5 0 0 0 1 0z"/>
                                    <path d="M14.5 3a1 1 0 0 1-1 1H13v9a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V4h-.5a1 1 0 0 1-1-1V2a1 1 0 0 1 1-1H6a1 1 0 0 1 1-1h2a1 1 0 0 1 1 1h3.5a1 1 0 0 1 1 1zM4.118 4 4 4.059V13a1 1 0 0 0 1 1h6a1 1 0 0 0 1-1V4.059L11.882 4zM2.5 3h11V2h-11z"/>
                                </svg>
                        </button>
                        <div class="d-flex flex-column flex-grow-1">
                            <input class="form-control" id="<%= agenda_id %>" type="text" name="<%= agenda_id %>" placeholder="<%= agenda.nom %>" value="<%= agenda.nom %>" required>
                            <div class="invalid-feedback">
                                Veuillez entrer un nom
                            </div>
                        </div>
                    </div>
                </div>
                <% } %>
                <div class="d-flex justify-content-center">
                    <button class="btn btn-primary me-2" type="submit">Confirmer</button>
                    <button class="btn btn-secondary ms-2" type="button" onclick="window.location.href = '/'">Annuler</button>
                </div>
        </form>
    </div>
    <script src="/bootstrap/js/bootstrap.bundle.min.js"></script>
    <script src="/js/modifierAgenda.js"></script>
</body>

</html>